React -> react-dom - if we are develop web application then use react-dom
         react-native -if mobile application develop then use react native

node package manager:
create project:
1. node package executor:npx create-react-app
2.vite
  1. npm create vit@latest

  All about virtual dom:
  createRoot method also create like DOM CALLED AS VIRTUAL DOM
  ONCE LOADING RENDER REPENT STRTUCUTRE

BEFORE VIRTUAL DOM-EVERY TIME CHNAGE USER THEN EVREY TIME RENDER APPLICATION
  VIRTUAL DOM - ONLY REQUIRED CHNAGE CHANGES GOES TO VIRTUAL DOM 

React Fiber:
  React fiber is ongoing reimplementation Reacts core algorithm

features:
Reconsilation: Reacts algorithm
this is uses to  differentiation one tree with another determine which part to be changed algorithm DOM - VIRTUAL DOM

Reconsilation is algorithm behind what populary understood as the virtual dom
