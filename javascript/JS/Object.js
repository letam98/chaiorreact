 const obj = {
  name: "Ketan",
  greet: () => {
    
    console.log(obj.name);
  }
};
 
obj.greet();



const name ={
  
name:"ketan",
sirName:"kanoje",
city:"pune",

showDetails: function()
{
console.log(`my name  is ${name.name} ${name.sirName} from ${name.city}`);
}
}

name.showDetails();