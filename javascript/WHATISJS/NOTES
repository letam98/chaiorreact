JavaScript is a programming language initially designed to interact with elements of web pages. Within web browsers, JavaScript consists of three main parts:

ECMAScript provides the core functionality.
The Document Object Model (DOM) provides interfaces for interacting with elements on web pages
The Browser Object Model (BOM) provides the browser API for interacting with the web browser.

what is client side rendering?
When JavaScript is used on a web page, it is executed in web browsers, serving as a client-side language.

what is server side rendering?
JavaScript can run on both web browsers and servers. A popular JavaScript server-side environment is Node.js.

let:
we cannot re-declare in the same block or global scope
var:
this variable declare same name its possible in same block global or block

syntax:
declaration - _$ a-z, A-Z 0-9

let f1 = 'apple',
    f2 = 'Banana';
let result = f2 < f1;
console.log(result); // true

comparing with ascii character value

A-Z =65-90
a-z =97-122

funtions :

function add()
{

}
let res =add =>calling function
let res =add() => then output come like NAN
let 

annonymous funtion =

((funtion) {  -> declaration
    console.log('hello ') -print
})() -return the data

Arrow function -ES6 FEATURE 
let show = () => log()

in object we can create funtion multiple ways:
1.getFullName: function()
2. getFullName()


let a={

}
for(let prop in person) = use for object

let a=[23,44,32]
for(let b of a) = use for object

It is used to call a function and manually set this==current method


In real apps:

You get dynamic data (API, DB)
Same function should work for all objects

👉 So:
One function + multiple objects = call()

apply - same as call but argument pass in array []

bind:
bind() = set this + return a NEW function




